<!DOCTYPE html>
<html>
  <head>
    <title>MembersOnly</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if flash[:notice] %>
      <div class="notification is-primary global-notification">
        <p class="notice"><%= notice %></p>
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="notification is-danger global-notification">
        <p class="alert"><%= alert %></p>
      </div>
    <% end %>

    <nav class="navbar" role="navigation" aria-label="main navigation">
      <div class="navbar-brand">
        <a class="navbar-item">
          <i class="far fa-comments"></i>
        </a>

        <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
        </a>
      </div>

      <div id="navbarBasicExample" class="navbar-menu">
        <div class="navbar-start">
          <a class="navbar-item" href="<%= root_path %>">
            Home
          </a>
          <a class="navbar-item" href="/pages/documentation">
            About
          </a>
        </div>

        <div class="navbar-end">
          <div class="navbar-item">
            <% if !member_signed_in? %>
            <div class="buttons">
              <a class="button is-primary is-small" href="<%= new_member_registration_path %>">
                <strong>Sign up</strong>
              </a>
              <a class="button is-light is-small" href="<%= new_member_session_path %>">
                Log in
              </a>
            </div>
            <% else %>
            <div class="">
              <%= link_to('Logout', destroy_member_session_path, method: :delete) %>
            </div>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <%= yield %>
  </body>
</html>

<%= javascript_pack_tag 'highlight_box' %>
<%= javascript_pack_tag 'follow_tags' %>
<%= javascript_pack_tag 'dropdown_comment' %>
<%= javascript_pack_tag 'show_like_tags' %>
<%= javascript_pack_tag 'toggle_modal' %>
<%= javascript_pack_tag 'show_member_tabs' %>
<%= javascript_pack_tag 'toggle_media_active' %>
<%= javascript_pack_tag 'show_member_profile_followers_following' %>
