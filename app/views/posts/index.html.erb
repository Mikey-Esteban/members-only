<section class="section">
  <div class="container">
    <div class="columns">
      <%= render 'quotes' %>
      <%= render 'feed' %>
      <% if member_signed_in? %>
        <%= render 'profile' %>
      <% else %>
      <div class="column is-one-third-desktop is-one-third-tablet">
        <%= render 'trending' %>
      </div>
      <% end %>
    </div>
  </div>
</section>
