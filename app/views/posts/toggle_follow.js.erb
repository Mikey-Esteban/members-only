// var postId = '<%= @post.id %>';
//
//
// function toggleFollowButton(postId, innerHTML) {
//   let followButton = document.querySelector(`.follow-icon[data-id='${postId}']`)
//   let svgButton = followButton.querySelector("svg")
//   let el = svgButton['dataset']['prefix']
//
//   el == 'fas' ? svgButton.setAttribute("data-prefix", 'far') : svgButton.setAttribute("data-prefix", 'fas')
// }
//
// toggleFollowButton(postId);
